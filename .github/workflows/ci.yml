# name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# concurrency:
#   group: ${{ github.workflow }}-${{ github.ref }}
#   cancel-in-progress: true

# jobs:
#   lint:
#     name: Lint
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: pnpm/action-setup@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 22
#           cache: pnpm
#       - run: pnpm install --frozen-lockfile
#       - run: pnpm lint

#   typecheck:
#     name: Typecheck
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: pnpm/action-setup@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 22
#           cache: pnpm
#       - run: pnpm install --frozen-lockfile
#       - run: pnpm --filter @commandra/shared build
#       - run: pnpm --filter @commandra/api exec tsc --noEmit
#       - run: pnpm --filter @commandra/extension exec tsc --noEmit

#   test:
#     name: Test
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: pnpm/action-setup@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 22
#           cache: pnpm
#       - run: pnpm install --frozen-lockfile
#       - run: pnpm --filter @commandra/shared build
#       - run: pnpm test

#   build:
#     name: Build
#     runs-on: ubuntu-latest
#     needs: [lint, typecheck, test]
#     steps:
#       - uses: actions/checkout@v4
#       - uses: pnpm/action-setup@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 22
#           cache: pnpm
#       - run: pnpm install --frozen-lockfile
#       - run: pnpm --filter @commandra/shared build
#       - run: pnpm --filter @commandra/api build
#       - run: pnpm --filter @commandra/extension build
#       - run: NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${{ secrets.CLERK_PUBLISHABLE_KEY }} CLERK_SECRET_KEY=${{ secrets.CLERK_SECRET_KEY }} NEXT_PUBLIC_API_URL=http://127.0.0.1:3001 pnpm --filter @commandra/web build
#         # web uses Clerk; point secrets at your Clerk test keys (or same vars as local .env.local)

#   docker:
#     name: Docker Build
#     runs-on: ubuntu-latest
#     needs: [build]
#     steps:
#       - uses: actions/checkout@v4
#       - run: docker build -t commandra-api ./apps/api
