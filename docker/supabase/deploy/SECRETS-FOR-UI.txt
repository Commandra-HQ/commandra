# Set these in Fly dashboard (Secrets) per app. Use same values as docker/supabase/.env.

commandra-db:
  POSTGRES_PASSWORD, JWT_SECRET, POSTGRES_PORT=5432, POSTGRES_DB=postgres, JWT_EXPIRY=3600

commandra-supavisor:
  POSTGRES_PASSWORD, SECRET_KEY_BASE, VAULT_ENC_KEY, API_JWT_SECRET (= JWT_SECRET), METRICS_JWT_SECRET (= JWT_SECRET), POOLER_TENANT_ID=commandra, POSTGRES_DB=postgres, POSTGRES_PORT=5432

commandra-meta:
  POSTGRES_PASSWORD, CRYPTO_KEY (= PG_META_CRYPTO_KEY from .env)

commandra-analytics:
  POSTGRES_PASSWORD, LOGFLARE_PUBLIC_ACCESS_TOKEN, LOGFLARE_PRIVATE_ACCESS_TOKEN, POSTGRES_BACKEND_URL (= postgresql://supabase_admin:POSTGRES_PASSWORD@commandra-db.internal:5432/_supabase)

commandra-studio:
  POSTGRES_PASSWORD, PG_META_CRYPTO_KEY, SUPABASE_PUBLIC_URL (= https://db.commandra.app), SUPABASE_ANON_KEY (= ANON_KEY), SUPABASE_SERVICE_KEY (= SERVICE_ROLE_KEY), AUTH_JWT_SECRET (= JWT_SECRET), LOGFLARE_PUBLIC_ACCESS_TOKEN, LOGFLARE_PRIVATE_ACCESS_TOKEN, STUDIO_DEFAULT_ORGANIZATION=Default, STUDIO_DEFAULT_PROJECT=Default

commandra-kong:
  DASHBOARD_USERNAME (= admin), DASHBOARD_PASSWORD, SUPABASE_ANON_KEY (= ANON_KEY), SUPABASE_SERVICE_KEY (= SERVICE_ROLE_KEY)
